version: 2

models:
  - name: customers
    description: "One record per customer with order and payment summary"
    columns:
      - name: customer_id
        description: "Primary key"
        tests:
          - unique
          - not_null
      - name: first_name
        tests:
          - not_null
      - name: number_of_orders
        tests:
          - not_null
      - name: customer_lifetime_value
        tests:
          - not_null
